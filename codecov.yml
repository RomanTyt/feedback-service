coverage:
  status:
    project:
      default:
        target: 80%    # the required coverage value
        threshold: 5%  # the leniency in hitting the target

ignore:
  - "feedback-service/src/main/java/com/tyutyakov/feedbackservice/exception/error/exception"
