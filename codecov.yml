coverage:
  status:
    project:
      default:
        target: 80%    # the required coverage value
        threshold: 5%  # the leniency in hitting the target

ignore:
  - "src/main/java/com/tyutyakov/feedbackservice/exception/error/exception"
  - "src/main/java/com/tyutyakov/feedbackservice/exception/error/AppError.java"
